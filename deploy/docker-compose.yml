services:
  web:
    build:
      context: ../server
      network: host
    env_file:
      - ../.env
    volumes:
      - ../data:/app/data
    ports:
      - "127.0.0.1:8610:8000"
    restart: unless-stopped
